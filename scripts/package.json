{
  "name": "pipeline-scripts",
  "version": "1.0.0",
  "description": "Pipeline plugin knowledge DB scripts",
  "type": "commonjs",
  "scripts": {
    "setup": "node pipeline-db.js setup",
    "status": "node pipeline-db.js status",
    "test": "echo \"No tests\" && exit 0"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
